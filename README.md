# AI Technical Interviewer v2 - Multi-Agent System

Система технического интервью с 3 AI-агентами, SQLite базой задач, адаптивной сложностью и системой штрафов.

## 🤖 Агенты

| Агент | Роль | Фокус |
|-------|------|-------|
| 👩‍💼 **Анна** | HR Manager | Soft skills, коммуникация, мотивация |
| 👨‍💻 **Дмитрий** | Tech Lead | Архитектура, код-ревью, технические решения |
| 🧑‍💻 **Максим** | Senior Dev | Качество кода, алгоритмы, best practices |

## 🚀 Быстрый старт

```bash
# 1. Установка зависимостей
pip install fastapi uvicorn openai

# 2. Инициализация БД и запуск
python backend.py

# 3. Открыть frontend.html в браузере
```

## 📊 База данных (SQLite)

Автоматически создаётся при первом запуске:

- **coding_tasks** — задачи на код (junior/middle/senior)
- **task_tests** — тесты для задач
- **theory_questions** — теоретические вопросы
- **interview_sessions** — история интервью
- **candidate_answers** — ответы кандидатов

## 🎯 Функциональность

### Адаптивная сложность
LLM через function calling выбирает задачи из БД:
- При высоком скоре → сложнее
- При низком скоре → проще

### Система штрафов

| Штраф | Баллы |
|-------|-------|
| Неправильный ответ | -5 |
| Timeout | -10 |
| Подсказка | -3 |
| Каждая доп. попытка | -2 |
| Плохая коммуникация | -5 |

### Фазы интервью

1. **Intro** — знакомство с HR
2. **Theory** — теоретические вопросы
3. **Coding** — задачи на код
4. **Review** — обсуждение результатов
5. **Final** — совещание агентов

### Финальное совещание

Все 3 агента:
1. Анализируют результаты
2. Дают индивидуальную оценку
3. Голосуют за решение
4. Предоставляют обратную связь

Решение: `STRONG HIRE` / `HIRE` / `MAYBE` / `NO HIRE`

## 🛠 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/start` | Начать интервью |
| GET | `/api/task` | Текущая задача на код |
| GET | `/api/theory` | Текущий теор. вопрос |
| POST | `/api/submit-code` | Отправить код |
| POST | `/api/submit-theory` | Отправить ответ на теорию |
| POST | `/api/chat` | Сообщение агенту |
| POST | `/api/hint` | Подсказка (со штрафом) |
| POST | `/api/switch-agent` | Сменить агента |
| POST | `/api/finish` | Завершить и получить отчёт |
| GET | `/api/status` | Статус интервью |
| GET | `/api/agents` | Список агентов |

## 🔧 Function Calling Tools

LLM может вызывать:
- `get_next_task` — получить задачу (с настройкой сложности)
- `get_theory_question` — получить теор. вопрос
- `evaluate_theory_answer` — оценить ответ
- `add_penalty` — добавить штраф
- `add_agent_note` — добавить заметку
- `switch_agent` — сменить интервьюера
- `change_phase` — сменить фазу
- `finish_interview` — завершить

## ⚙️ Настройка LLM

В `backend.py`:
```python
API_KEY = "ваш-ключ"
API_BASE_URL = "https://ваш-сервер/v1"
MODEL_NAME = "ваша-модель"
```

## 📁 Структура

```
ai-interviewer-v2/
├── backend.py       # FastAPI + агенты + function calling
├── database.py      # SQLite БД + задачи
├── frontend.html    # React-like UI
├── interviewer.db   # БД (создаётся автоматически)
└── README.md
```

## 📝 Добавление задач

В `database.py` в функции `seed_coding_tasks()`:

```python
{
    "level": "middle",
    "difficulty": 5,
    "title": "Название задачи",
    "description": "Описание...",
    "examples": "solution(1) → 1",
    "starter_code": "def solution():\n    pass",
    "hints": json.dumps(["Подсказка 1", "Подсказка 2"]),
    "tags": json.dumps(["arrays", "dp"]),
    "time_limit": 10,
    "tests": [
        {"input": [1], "expected": 1, "hidden": False},
        {"input": [2], "expected": 4, "hidden": True},
    ]
}
```

## 🔄 Сброс БД

```bash
rm interviewer.db
python backend.py  # создаст заново
```
